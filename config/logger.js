// Настройки сбора логов
const loggerSettings = {
  // Файл (путь к файлу) всех запросов к api
  requestFile: 'request.log',

  // Файл (путь к файлу) всех ошибок, возникающих при работе api
  errorFile: 'error.log',
};

module.exports = loggerSettings;
